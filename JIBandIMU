private static DataSetIterator getDataSetIterator(String folderPath, int nSamples)  {
	/*	
		File folder = new File(folderPath);
		File[] imuFolder = folder.listFiles();
		ArrayList<Double> rpy = new ArrayList<Double>();
		
		try {
			Scanner imuScan = new Scanner(folder);
			while (imuScan.hasNextLine()) 
			{
				folder.add(imuScan.nextLine());
			}
		}
		
		catch (FileNotFoundException s)
		{
			s.printStackTrace();
		}
	*/
		INDArray input = Nd4j.create(new double[] { nSamples, COLUMNS*ROWS});
		INDArray output = Nd4j.create(new double[] {nSamples, N_OUTCOMES});
		
		int n = 0;
		for (File imuFolder : imuFolders)
		{
			String scrubData = String.parse(imuFolder.getName);
		File[] imuFiles = imuFolder.listFiles();
		for (File imuFile : imuFiles)
		}
	
	
	DataSet dataSet = new DataSet( input , output );
	List<DataSet> listDataSet = dataSet.asList();
	int batchSize = 10;
	DataSetIterator dsi = new ListDataSetIterator<DataSet>(listDataSet, batchSize);
	return dsi;
	}
